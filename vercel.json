{
  "version": 2,
  "builds": [
    {
      "src": "index.tsx",
      "use": "@vercel/static-build",
      "config": {
        "buildCommand": "npm run build",
        "outputDirectory": ".vercel/output"
      }
    }
  ],
  "routes": [
    {
      "src": "/",
      "dest": "index.html"
    }
  ]
}
